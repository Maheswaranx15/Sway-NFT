contract Test {
    int x;

    public function equal(int y) {
        require(y == this.x);
    }

    public function smaller(int y) {
        require(y < this.x);
    }

    public function larger(int y) {
        require(y > this.x);
    }

    public function smalfler(int y) {
        require(y < this.x);
    }

    public function largfer(int y) {
        require(y > this.x);
    }
}